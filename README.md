#Subtasks for this branch were completed 
prompt() replaces the hardcoded values, the code is split into functions, objects store the advice, comments explain the sections, and it recommends plants for each season.
